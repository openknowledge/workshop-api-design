events {
}

http {

  server {
    listen 8080;
    root /var/www;

    location /resources/ {
      proxy_pass http://keycloak:8080;
    }

    location /realms {
      proxy_pass http://keycloak:8080;
      proxy_set_header Host ${CODESPACE_HOST};
    }
  }
}
